name: Go

on:
  push:
    branches: [ PBI-2-dbmanagelink ]
  pull_request:
    branches: [ PBI-2-dbmanagelink ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.16

    - name: Test
      run: go test -v ./auth